{
  "name": "k3s-status-dashboard",
  "version": "1.0.0",
  "description": "A tiny, zero-dependency-config status dashboard for Kubernetes / k3s. Reads Ingress hosts and nodes straight from the Kubernetes API and shows them on a single clean page.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "license": "MIT",
  "dependencies": {
    "@kubernetes/client-node": "0.21.0",
    "express": "^4.19.2"
  }
}
