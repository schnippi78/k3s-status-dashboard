{
  "name": "k3s-status-dashboard",
  "version": "1.0.0",
  "description": "A tiny status dashboard for Kubernetes / k3s. Actively probes your services (HTTP/TCP) and reads node health from Prometheus, on a single clean page. No cluster API access required.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2"
  }
}
